SUPABASE CONFIG FILE UPDATES NEEDED
=====================================

File: zentrohomes.com/js/supabase-config.js

1. ADD STORAGE CONFIGURATION
----------------------------
Location: After line 18 (after the tables object closing brace), before line 19 (the closing brace of SUPABASE_CONFIG)

FIND THIS:
    adminSessions: 'admin_sessions'
  }
};

REPLACE WITH:
    adminSessions: 'admin_sessions'
  },
  
  storage: {
    buckets: {
      images: 'property-images',
      videos: 'property-videos',
      thumbnails: 'property-thumbnails'
    },
    maxFileSizes: {
      image: 10 * 1024 * 1024, // 10MB
      video: 100 * 1024 * 1024  // 100MB
    },
    allowedTypes: {
      images: ['image/jpeg', 'image/png', 'image/webp', 'image/gif'],
      videos: ['video/mp4', 'video/webm', 'video/mov']
    }
  }
};

2. ADD MEDIA MANAGER INITIALIZATION
-----------------------------------
Location: After line 495 (after the existing DOM content loaded event), before the export section

FIND THIS:
});

// Export for use in other files

ADD AFTER THE }); AND BEFORE THE EXPORT COMMENT:

// Initialize media manager
let mediaManager;
document.addEventListener('DOMContentLoaded', function() {
    if (supabase) {
        mediaManager = new SupabaseMediaManager(supabase);
    }
});

SUMMARY
=======
1. ✅ Media Manager Class created: zentrohomes.com/js/supabase-media-manager.js
2. ⏳ Update supabase-config.js with storage configuration (lines 17-18)
3. ⏳ Add media manager initialization to supabase-config.js (after line 495)

The Media Manager is ready to use once you make these config updates!